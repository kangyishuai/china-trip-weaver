interface:
  display_name: "Search China Rail"
  short_description: "Normalize dated 12306 rail candidates"
  default_prompt: "Use $search-china-rail to find read-only dated rail candidates for this leg."
policy:
  allow_implicit_invocation: false
