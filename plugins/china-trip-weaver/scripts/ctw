#!/usr/bin/env python3
"""Repository-local launcher that needs no installation or virtualenv."""

import sys
from pathlib import Path


PLUGIN_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(PLUGIN_ROOT / "src"))

from china_trip_weaver.cli import main  # noqa: E402


raise SystemExit(main())

