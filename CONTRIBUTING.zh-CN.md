# 参与贡献

[English](CONTRIBUTING.md) · **简体中文**

## 两条不可协商的底线

这是一个只读的旅行规划插件。

1. **永远不要加入交易能力。** 不登录、不提交身份信息、不占位库存、不下单、不支付、不取消、不改签。只做查询、比较，然后给出指向官方页面的链接。
2. **永远不要让凭据离开服务商进程。** 它不能出现在命令行参数、日志、测试夹具、Trip 文档、渲染出的 HTML 或 Git 里。详见 [`SECURITY.zh-CN.md`](SECURITY.zh-CN.md)。

## 本地检查

运行时只用 Python 标准库，目标版本是 Python 3.9。Node 仅用于通过 `npx` 启动固定版本的 MCP 与 CLI 服务商，不会全局安装任何东西。

```bash
python3 -m unittest discover -s tests -v
python3 scripts/scan_secrets.py
```

测试必须以 `OK` 结束、零失败。有两项测试会调用 Codex 自带的 Skill 与插件校验器：机器上没装 Codex 时它们跳过，装了就必须通过。

## 一个改动需要带上什么

- 一个改动前会失败、改动后才通过的测试。不要削弱已有断言、不要删测试、不要用跳过来凑绿。
- 你碰到的每种失败模式都要有服务商夹具：成功、空结果、鉴权失败、限流、超时、形状漂移。只有成功样例的服务商不算被覆盖。
- 如实降级。静态估算不能当作实时路线，遮罩价不能当作数字，未知不能写成零。

## 架构决策

`docs/design/` 是已接受的架构，`docs/design/adr/` 是决策记录。如果一个改动与其中某条冲突，请新增一份编号的 ADR 来取代它，而不是悄悄改实现。

## 服务商凭据

凭据是可选的，无 Key 的基线必须始终可用。不要提交凭据文件，也不要把值粘贴进 issue、Pull Request 或与 AI 助手的对话里。
