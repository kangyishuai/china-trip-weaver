# 安全策略

[English](SECURITY.md) · **简体中文**

## 报告漏洞

请通过 GitHub 仓库的 Security 标签页提交私密安全通告。不要用公开 issue 报告漏洞，也绝对不要在报告里附上真实的 API Key、令牌、Cookie 或个人订单信息。

## 本项目如何处理凭据

本插件把服务商凭据当作只进不出的输入。

- 值只来自启动进程的环境变量，或 `~/.config/china-trip-weaver/credentials.env`。该文件必须是当前用户拥有的普通文件，权限恰为 `0600`。
- 每个服务商子进程只拿到属于自己的变量。排程器、渲染器和铁路服务商一个也拿不到。
- 凭据不会出现在命令行参数、日志、缓存夹具、Trip 文档、渲染出的 HTML 或 Git 中。`ctw doctor` 只打印 `configured` 或 `missing`，绝不打印值、前缀、后缀、哈希或长度。
- `scripts/scan_secrets.py` 负责强制这一点。加 `--credential-values` 用你自己配置的真实值检查工作区，再加 `--git-history` 检查每一个已提交的 blob。两者都必须报告零命中。

如果你发现任何一条路径会把凭据泄露到上述任一处，请按漏洞处理并私密报告。

## 插件的能力边界

插件在设计上就是只读的。它永远不会登录、提交身份证件、占位库存、下单、支付、取消或改签。任何增加交易能力的改动都属于安全相关改动，需要单独的架构决策记录。
